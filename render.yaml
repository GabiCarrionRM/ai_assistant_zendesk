# Exported from Render on 2025-04-11T00:28:37Z
services:
- type: web
  name: ai_assistant_zendesk
  runtime: python
  repo: https://github.com/GabiCarrionRM/ai_assistant_zendesk
  plan: starter
  envVars:
  - key: LANGFLOW_API_KEY
    sync: false
  region: oregon
  buildCommand: pip install -r requirements.txt
  startCommand: langflow run --host 0.0.0.0 --port $PORT
version: "1"
